TODO:

  Don't automatically update stripe subscriptions.
  If the subscription requires the creation of a new subscription, for example — single page (yearly) to multi page (monthly) — create a new subscription in stripe.

  Don't automatically create new db subscriptions.
  If a subscription requires an update and not the creation of a new subscription, for example — all except single page (yearly) to multi page (monthly), update the existing db subscription rather than creating a new one.

  Have the start giveaway modal pop back up on the final step if the user is referred back after successful subscription.

  SG.UI flash messages function.

  Create a page for selecting plans that is not tied to giveaway flow or facebook page. In this case you will need to allow user to select a page for single page plans.

  Create a page for a user to manage his subscriptions.
