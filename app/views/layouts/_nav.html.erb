<nav class="clearfix">
	<span class="root">
		<%= link_to "SGA", root_path %>
	</span>
	<div class="session">
	  <% if user_signed_in? %>
			<div class="current-user">Signed in as <%= link_to current_user.name, user_path(current_user) %>. Not you?</div>
			<div class="button-wrapper">
		    <span class="button pill">
					<%= link_to "Sign out", destroy_user_session_path %>
				</span>
			</div>
	  <% else %>
      <a href="/auth/facebook" class="auth-provider facebook clearfix">Facebook</a>
	  <% end %>
	</div>
</nav>