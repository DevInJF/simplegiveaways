#step_one.modal-step{:'data-modal-step' => 1}
  .ui.grid
    = render 'giveaways/avatars', giveaway: giveaway
    .row
      .sixteen.wide.column
        - if facebook_page.no_active_giveaways?
          = render 'giveaways/form/publish_confirm', giveaway: giveaway
        - else
          = render 'giveaways/pending/active_conflict', giveaway: giveaway
