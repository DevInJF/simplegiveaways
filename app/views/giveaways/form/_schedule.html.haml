.row.schedule

  %h4.ui.header
    Schedule
    %p.sub.header
      If you choose a start date and/or end date, we will automatically publish and/or unpublish your giveaway at the chosen date and time (as long as there is not a scheduling conflict with another giveaway).

  .date-container
    = f.input :start_date, as: :string, input_html: { value: f.object.start_date && f.object.start_date.in_time_zone, class: 'datetime-picker', disabled: f.object.active?, 'data-date-type' => 'start' }, wrapper_html: { class: 'field date' }, label: 'Start Date/Time'
    .conflicts-container.ui.pointing.above.label{style: 'display: none'}
      This start date conflicts with the following giveaway schedules:
      %br

  .date-container
    = f.input :end_date, as: :string, input_html: { value: f.object.end_date && f.object.end_date.in_time_zone, class: 'datetime-picker', 'data-date-type' => 'end' }, wrapper_html: { class: 'field date' }, label: 'End Date/Time'
    .conflicts-container.ui.pointing.above.label{style: 'display: none'}
      This end date conflicts with the following giveaway schedules:
      %br
