.bs-callout.bs-callout-info
  %h4 Scheduled Giveaways
  %p If you choose a start date and/or end date, we will automatically publish and/or unpublish your giveaway at the chosen date and time (as long as there is not a scheduling conflict with another giveaway).

#giveaway_start_date.date-container{ 'data-date' => f.object.start_date && f.object.start_date.in_time_zone }
  = f.input :start_date, as: :string, input_html: { value: f.object.start_date && f.object.start_date.in_time_zone, class: 'datetime-picker-input', disabled: f.object.active?, 'data-date-type' => 'start' }, wrapper_html: { class: 'field date' }, label: 'Start Date/Time'
  %input.date-trigger.hide{type: "text", value: "#{f.object.start_date && f.object.start_date.to_formatted_s(:date)}"}/
  %input.time-trigger.hide{type: "text", value: "#{f.object.start_date && f.object.start_date.to_formatted_s(:time)}"}/
  .pickadate-outlet
  .conflicts-container.ui.pointing.above.label{style: 'display: none'}
    .conflict-message
      %i.info.icon
      This start date conflicts with the following giveaway schedules.
      %br
      If you choose to keep this date, the current giveaway will not be automatically published.

#giveaway_end_date.date-container{ 'data-date' => f.object.end_date && f.object.end_date.in_time_zone }
  = f.input :end_date, as: :string, input_html: { value: f.object.end_date && f.object.end_date.in_time_zone, class: 'datetime-picker-input', 'data-date-type' => 'end' }, wrapper_html: { class: 'field date' }, label: 'End Date/Time'
  %input.date-trigger.hide{type: "text", value: "#{f.object.end_date && f.object.end_date.to_formatted_s(:date)}"}/
  %input.time-trigger.hide{type: "text", value: "#{f.object.end_date && f.object.end_date.to_formatted_s(:time)}"}/
  .pickadate-outlet
  .conflicts-container.ui.pointing.above.label{style: 'display: none'}
    .conflict-message
      %i.info.icon
      This end date conflicts with the following giveaway schedules.
      %br
      If you choose to keep this date, the conflicting giveaways will not be automatically published.
