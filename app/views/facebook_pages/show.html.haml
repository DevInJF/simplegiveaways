- content_for(:page_heading) do
  = @page.name

= render "navigation"

.tab-content.span14
  #page_overview.tab-pane.active
    = render "overview"
  #page_giveaways_active.tab-pane
    = render :collection => Giveaway.active, :partial => "giveaway"
  #page_giveaways_pending.tab-pane
    = render :collection => Giveaway.pending, :partial => "giveaway"
  #page_giveaways_completed.tab-pane
    = render :collection => Giveaway.completed, :partial => "giveaway"

